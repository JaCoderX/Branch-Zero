# Kickoff prompt — U7 ship reel (demo video)

Paste into a **new** Claude Code / Cursor agent session. Prefer **Fable 5.1**. Escalate only if the recording
harness itself is broken after one honest attempt.

**What this is:** the next *visual* unit after the Stage 1–5 art ladder — a **continuous ≥30 s demo reel** of the
dressed bank on MockChain (or live if already green). Stages 1–5 are **met**. This is **not** a sixth art class
(no new kits, HUD rewrite, shell, characters), **not** U6 Arc construction, **not** ETHOnline zip packaging
(principal-owned unless this brief is extended).

**Ladder:**

| Stage | Goal | Status |
|-------|------|--------|
| 1–5 | Fill → heroes → characters → shell → feel | **met** |
| **Ship reel** | **Continuous desk-circuit video** | **this kickoff** |
| Ship package | Title cards, store page, submission zip | principal |

**Baseline after Stage 5** ([`progress/2026-09-07-u7-viz-stage5.md`](./progress/2026-09-07-u7-viz-stage5.md)):
feel landed (HUD bottom band, OTS + talk two-shot, dust/ink, Inter/Cinzel); worst ~281 draws / 144k prims / 36 mats;
frame-stamped `:5174` circuit already proves the loop — judges still need **motion**.

**Sibling assets (may already exist uncommitted):** `scripts/record-demo-walk.ps1`, `apps/game/scripts/demo_walk.gd`.
Prefer finishing and documenting that path over inventing a second harness.

**Lessons:** path-scoped OWN; hunk subset on contested `player.gd`; capture evidence can show sibling dirt; shoulder
offset alone cannot clear ahead NPCs; bake > MultiMesh; spring-arm shot clearance.

**Timing / ports:** schedule clear of U6 Arc funding / elevator string edits if you need clean pixels; otherwise note
dirt. Prefer `:5174?mock=account` for a reliable reel; leave `:5173` / `:8787` unless the principal asks for live.

```text
You are a cold agent. No prior chat. Prefer docs over memory.

MISSION: Branch Zero — U7 ship reel ONLY (continuous ≥30 s demo video of the desk circuit).
Freedom on HOW. No freedom on constraints.
This is NOT Stage 6 art, NOT HUD/camera redesign, NOT U6 Arc, NOT submission zip packaging.

BEFORE CODE — read in order:
1. https://github.com/JaCoderX/Branch-Zero/blob/main/docs/HANDOFF-CC.md
2. https://github.com/JaCoderX/Branch-Zero/blob/main/docs/KICKOFF-U7-ship-reel.md
3. https://github.com/JaCoderX/Branch-Zero/blob/main/docs/progress/2026-09-07-u7-viz-stage5.md
4. https://github.com/JaCoderX/Branch-Zero/blob/main/docs/DEV-LOOP.md
5. Craft lessons:
   https://github.com/D9-Studio/GameDevOS/blob/main/wiki/lessons/path-scoped-stage-owned-files.md
   https://github.com/D9-Studio/GameDevOS/blob/main/wiki/lessons/stage-hunk-subset-of-contested-files.md
   https://github.com/D9-Studio/GameDevOS/blob/main/wiki/lessons/capture-evidence-can-show-sibling-dirt.md
   https://github.com/D9-Studio/GameDevOS/blob/main/wiki/lessons/shoulder-offset-alone-cannot-clear-ahead-npc.md

Local root: D:\My Git Projects\D9-Studio\Branch-Zero

HARD RULES:
- Deliver a continuous screen recording (≥30 s, prefer 60–180 s) of: lobby → talk Mo → Counter wire path → vault
  door read (PENDING and/or OPEN). MockChain on :5174 is enough unless principal requires live.
- Prefer completing existing demo_walk / record-demo-walk.ps1 over a new framework.
- Do NOT rewrite hud.gd, feel particles, fonts, shell, characters, or heroes. Camera hunks in player.gd only if the
  reel harness needs a documented hook — stage hunk subset if autopilot and feel share the file.
- Do NOT touch Arc funding keys, teller-desk product logic, ens*, or bridge semantics.
- Path-scoped commits. Progress note + README index row + path to the video (or LFS / release asset link).
- Do NOT advance construction HANDOFF past U6 from this pass.

OWN: demo_walk.gd / record-demo-walk.ps1 (or equivalent), captures/video under docs/progress/, progress note + README.

AVOID: bank_interior shell, character atlas, hud layout redo, U6 Arc scripts, teller, web bridge.

DoD:
- One continuous reel ≥30 s with canvas focus; vault or ledger state is readable in motion.
- How-to-re-run documented in the progress note.
- Path-scoped commit; no Stage 1–5 art regression.

Stop when the reel DoD is met or a named harness blocker needs the principal.
```

## After this pass

Full U7 ship packaging (title cards, submission) stays principal-scheduled. Product mission remains **U6 Arc** until
HANDOFF says otherwise.
