---
date: 2026-09-07
unit: U7 early viz (art pass off the U4+ checkpoint — not the ship unit)
gate: early viz DoD (KICKOFF-U7-viz) — met
result: Main wing reads as a bank instead of CSG boxes; vault states legible from the lobby; freeze intact; budget held with room; every third-party asset in CREDITS.md
agent: Claude Code (Fable 5.1), cold session
---

# 2026-09-07 — U7 early viz: materials, lighting, kit props, CC0 characters

Scope was [`docs/KICKOFF-U7-viz.md`](../KICKOFF-U7-viz.md) only: dress the U4+ greybox in place. No lane, role,
dialogue, bridge, MockChain or layout change; U5's Name Desk interactables and Petra wiring were not touched (the
zone is dressed and waiting — see "Handover to U5"). HANDOFF still says U5.

Before: [`2026-09-07-u4-vault-clock-mock.gif`](./captures/2026-09-07-u4-vault-clock-mock.gif) (coloured boxes).
After: [lobby → vault](./captures/2026-09-07-u7-viz-lobby-to-vault.jpg) ·
[lobby, wire cooling](./captures/2026-09-07-u7-viz-lobby-pending.jpg) ·
[vault open](./captures/2026-09-07-u7-viz-vault-open.jpg) · [counter 1](./captures/2026-09-07-u7-viz-counter.jpg).

## What changed (apps/game)

| Area | Before (U3/U4+) | Now |
|------|-----------------|-----|
| Materials | flat `StandardMaterial3D` per colour, no theme | `scripts/wing_theme.gd` (`WingTheme` resource) + `themes/wing_main.tres`; `themes/wing_arc.tres` is a palette-notes stub for U6 (nothing loads it). Terrazzo floor is a procedural `NoiseTexture2D` (no texture file). Shared-material cache in `scripts/props.gd` keyed by colour/metallic/roughness so kit props do not multiply materials |
| Lighting (WORLD-3D §7) | one sun, ambient, fog | `scripts/lighting.gd` on `Lighting` in `main.tscn`: warm key (4800 K-ish) from the NE through a **lobby skylight** in a new ceiling, 2 PSSM splits; cool shadowless fill from the south; ACES, exposure 1.1, light fog, low glow (`hdr_threshold 1.3`) so the vault LED and pendant bulbs bloom. Eight pendant `OmniLight3D`s (shadows off) — one per zone; the Compatibility renderer lights ≤ 8 omnis per mesh and the merged interior sees all eight, so **do not add a ninth** |
| Architecture | boxes | same boxes, plus: deep-green wainscot + brass cap on every marble wall, brass cornice, ceiling slabs with a brass-framed skylight (glass casts no shadow), clerestory windows (emissive cool panes) on three walls, brass mullions + rail on the manager's glass, revolving-door rings/axis, rosette with green field, paper boards behind the wall plaques |
| Hero props (ours, Blender 4.5 headless, `tools/hero_props.py`) | cylinder + boxes | `assets/models/hero/prop_*.glb`: vault frame (LED strip + clock plate), vault door (bevelled steel disc, brass rings, hand wheel, hinge, **six radial bolts** that slide out of the rim), marble counter (green top, brass flutes + foot rail, glass with slot), split-flap board housings (ledger + names), engraver, dot-matrix printer, stamp, shredder, pendant lamp, water cooler, rope post, elevator panel. Materials in the .glb are palette *names*; `props.gd` swaps them for the theme |
| Kit props (Kenney Furniture Kit, CC0) | boxes / cylinders | benches with green cushions (2 modules per bench), potted plants (lobby ×3 at ×3, the Account Opening landmark at ×4), desks (manager, account opening, name desk), cushion chairs, screen + keyboard, table lamp, bookcases (brochure rack, magazine rack, manager), rugs, doormat, coat rack, side-door frame. Kit colours re-tinted to the palette (`kit_recolor`) |
| Characters (Kenney Mini Characters, CC0) | coloured capsules with a nose | rigged chibi humans, one skin per NPC (`npc.gd` `SKINS`; unlisted ids → `character-female-c`, so Petra gets a body for free) and the player (`character-female-b`). Clips: `idle`, `interact-right` while WORKING, `emote-no` while REFUSING, `walk` while ESCORTING; player `walk`/`sprint` by speed. Colliders are still the U3 capsules; escort code untouched |
| SFX (optional step 4) | none | `autoload/audio.gd` (`Audio`): Kenney CC0 one-shots at −14 dB on **real events only** — stage `signing` → stamp, `broadcasting` → printer, `pending`/`mined` → split-flap clack, `released` → bolt, `cancelled` → shredder, refused action → buzzer. No music, no loops, no timers |
| Draw calls | one per box | `PropKit.bake_static()` merges every static mesh under `BankInterior` into one `ArrayMesh`, one surface per material (267 surfaces → 27). Colliders, zones, labels, the vault door, the ledger quad and `NamesBoardQuad` stay separate |
| Vault legibility | clock on the door face, hidden when the door swung open | door radius 1.75 → 1.6 and the clock plate lifted to y 3.62, above the swing; LED strips wider; a **repeater LED strip on the lobby face of the vault lintel** shares the state material; "VAULT" sign moved onto the lintel so it never sits in front of the clock |

Signage fixes found while looking: the two Account Opening plaques faced the wall (yaw 0 at z ≈ 10.5, single-sided) and
were never visible — now yaw π. Manager's Office plaque raised so it does not cover Okafor.

## Freeze (all green)

| Check | Result |
|-------|--------|
| `godot --headless --path apps/game -s tests/run_checks.gd` | **PASS — 0 failures** (dialogue JSON, 92 error lines, Ruth/Okafor verb split) |
| `… -s tests/run_mock_walk.gd` | **PASS — 0 failures** (wire → Ruth refused → Okafor priority → NOT_COOLING → PRIORITY_CANCELLED) |
| `… -s tests/run_viz_budget.gd` (new) | **PASS** — every .glb loaded; 31 unique materials ≤ 40; 34,222 tris whole building ≤ 400k; 55 surfaces + 30 labels; 1 shadowed light; **39 static bodies all layer 1 / mask 0**; six zone volumes unchanged; manager door, vault opening, 5 escort waypoints, 5 teleports, spawn and 5 NPC homes clear of solid props |
| Web export on `:5174` `?mock=account` (Browser pane) | boots (`bridge ready after 2736 ms, session after 9053 ms (MockChain)`); click bank → `F6` teleports, `E` opens Mo, `Esc`, `F4` shows the dressed vault → **canvas focus intact**. No new Privy surface (mock). Console: six `GL_INVALID_FRAMEBUFFER_OPERATION … Attachment has zero size` warnings during the first frames only (canvas at zero size while adaptive resize settles), none afterwards; the Privy `frame-ancestors` CSP error is the `:5174` origin and expected — Privy is allowed on `:5173` only |
| Colliders | every prop that blocks keeps a `StaticBody3D` on layer 1 / mask 0 (U4+ lesson); rotated hero props get their collider box counter-rotated so the world footprint matches the greybox box (`props.gd` `place`) |

## Budget (WORLD-3D §6) — measured

`tests/viz_shots.tscn` (windowed, MockChain) saves the DoD views and prints `RenderingServer` counters; the draw-call
counter includes the shadow pass.

| View | Draw calls | Primitives |
|------|-----------:|-----------:|
| Entrance | 168 | 91k |
| Lobby | 165 | 91k |
| Lobby → vault (idle / pending / open / cancelled) | 148–150 | 90k |
| Counter 1 | 122 | 84k |
| Account Opening | 124 | 84k |
| Manager's office | 108 | 81k |
| Vault antechamber (idle / pending / open / done) | 149–150 | 95k |
| Name Desk | 115 | 82k |

Budget ≤ 350 draw calls, ≤ 400k tris, ≤ 40 materials, 1 shadowed light: **held with room**. Before merging, the same
views cost 273–800 draw calls (4 shadow splits × ~200 casters) — the merge and 2 splits are what brought it down.

| Export | U4 freeze | Now |
|--------|----------:|----:|
| `index.pck` | 144,756 B | **994,180 B** (+0.85 MB: 13 hero .glb 0.57 MB, 18 kit .glb, 7 characters + 512² colormap 8.7 KB, 6 ogg) |
| `index.wasm` | 38,047,590 B | 38,047,590 B (unchanged; brotli at the host is still the first-load story) |
| `index.js` | 305,185 B | 305,185 B |

Textures: one, `colormap.png` 512² (≤ 1024²), imported **lossless** with `detect_3d/compress_to=0` pinned — Basis
Universal would triple an 8.7 KB palette map and blur its colour edges; revisit if a real atlas arrives. Meshes import
with LODs on, lightmap UVs off (WORLD-3D §5).

## Licences

[`CREDITS.md`](../../CREDITS.md) (new) lists every third-party file with author, licence, source URL and zip sha256:
Kenney Furniture Kit 2.0, Kenney Mini Characters 1.0, Kenney Impact Sounds 1.0, Kenney Interface Sounds 1.0 — all
CC0; hero props and code ours (MIT). No Mixamo, no Quaternius (their site has no direct download link to fetch),
no fonts added yet (Godot default; OFL picks from WORLD-3D §5 are for the full U7).

## Handover to U5 (Name Desk)

- Static dress only: kit desk (collider 2.4 × 0.8 × 1.0 as before), engraver on the desk, plant, names board housing on
  the west wall. **`BankInterior/NamesBoardQuad`** (2.3 × 1.2 m `QuadMesh`, facing east at x = −14.77) is left as its
  own node with `no_batch` so U5 can put the registered-names `SubViewport` texture on it, the way `ledger_board.gd`
  does. Nothing stands north of the desk — Petra's spot is free.
- `npc.gd` `SKINS` has no `registrar` entry yet; add one (e.g. `character-female-c`) or accept the default.
- The "NAME DESK (opens with U5)" plaque is unchanged.

## Not done / carried forward

- **HUD overlap:** the zone chip (top centre) and the passbook (top left) cover part of the vault lintel signage and the
  door clock from the lobby camera spot; the state still reads from the LED strips and the repeater. Moving HUD elements
  is `hud.gd` — left for the full U7 pass.
- Arc wing: `wing_arc.tres` palette notes only; no theme switching, no elevator behaviour (U6).
- Ambient customers, particles (stamp ink, dust), camera polish, music, fonts: full U7 (G8).
- The greybox draw-call number before this pass was never measured; the 273–800 figure above is this pass before merging.
- `viz_shots.tscn` needs a window (headless does not render) — run it from a terminal, not the Browser pane.

## Code pointers

- `apps/game/scripts/props.gd` — `PropKit`: palette/material cache, `instance/place/kit/hero`, `character`, `bake_static`
- `apps/game/scripts/bank_interior.gd` — the interior, same footprints; `scripts/vault_door.gd` — hero door + bolts + repeater
- `apps/game/scripts/lighting.gd`, `scripts/wing_theme.gd`, `themes/wing_main.tres`, `themes/wing_arc.tres`
- `apps/game/autoload/audio.gd` (autoload `Audio`, added to `project.godot`)
- `apps/game/tools/hero_props.py` — regenerates `assets/models/hero/*.glb` with Blender 4.5
- `apps/game/tests/run_viz_budget.gd` (headless budget + freeze rules), `tests/viz_shots.tscn` (windowed captures)
- `.claude/launch.json` — `branch-zero-viz` starts the shell on `:5174` (`:5173` / `:8787` stay U5's)

## Next art unit

Stage 2 hero polish — [`docs/KICKOFF-U7-viz-stage2.md`](../KICKOFF-U7-viz-stage2.md) (vault door, counters, board
housings, elevator panel only; keep Kenney fill and Mini Characters until Stage 3–4).
