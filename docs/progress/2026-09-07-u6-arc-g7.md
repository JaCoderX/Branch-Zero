# U6 — Arc + manager role (G7) progress

Date: 2026-09-07
Status: **PARTIAL / named blocker** — Arc readback, chain guards, provision code, wing switch, and regressions are green. A fresh product-controlled Arc clone/manager kill is not claimed because this workspace has no funded `ARC_*` keys and the faucet is reCAPTCHA-gated.

## Scope kept

- Arc Testnet only: chain ID `5042002`; native gas is USDC in 18-decimal units, displayed with 6 decimals.
- Remote EVM `1337`, Sepolia ENS, bridge `u5.0`, Petra, the one Account Opening consent, and U4+ `ROLE_SET_VERSION=3` Priority remain intact.
- No GameLab ENG tree was merged. No Remote EVM volume was wiped. No runtime dependency was added: `@bloxchain/sdk` + `viem` remain the product path.

## Shipped

- Added `arcTestnet`, exact Arc constants, supported-chain mapping, native-USDC `formatUsdc`, and Arc-specific deployment selection in `packages/shared`.
- Seeded [`infra/deployments/arc-testnet.json`](../../infra/deployments/arc-testnet.json) from ENG-2026-0006 before product changes. It pins EngineBlox, SecureOwnableDefinitions, RuntimeRBACDefinitions, GuardControllerDefinitions, the K3 AccountBlox fixture, and the Arc native USDC interface. It deliberately does not invent a CopyBlox address.
- Infra scripts accept `--chain remote|arc`; Arc refuses any RPC reporting a chain ID other than `5042002` and refuses Ganache-parity keys. Arc bootstrap deploys CopyBlox from already-built protocol artifacts and never deploys a fake ERC-20 on Arc.
- Teller Desk is target-specific through `CHAIN_ID`: Remote uses existing keys/files, Arc uses `ARC_*` keys and `arc-testnet.json`. Player/receipt files are chain-namespaced so the two desks do not overwrite one another.
- Arc reuses the existing U4+ manager beat: owner signs Priority, Branch Manager submits before the clock; Ruth remains the timed owner release. Chain-namespaced Privy rules let one already-attached policy serve both wings without a second consent.
- The React bridge and Godot `GameState` have `switchWing`; the smallest elevator beat rebuilds the same bank layout with `wing_arc.tres` and updates the active Teller Desk. MockChain exposes the same switch as greybox only.
- Arc mined receipts now carry a human-readable fee (`$...`) derived from native 18-decimal gas; Remote receipts retain ETH formatting.

## Evidence

### Live Arc readback — PASS

Commands (run with network permission because the sandbox blocks outbound HTTPS):

```text
node --experimental-strip-types infra/scripts/probe.ts --chain arc
node --experimental-strip-types infra/scripts/arc-kill.ts
node --experimental-strip-types infra/scripts/smoke.ts --chain arc
```

Observed:

- `Arc Testnet`, `chainId 5042002`, client `arc/v1`, gas limit `30,000,000`.
- Native currency reports `USDC` with 18 decimals; `0x3600000000000000000000000000000000000000` reports `USDC`, 6 decimals.
- All four pinned libraries have live code.
- K3 AccountBlox [`0x07fb4b5d6b91f78163649d563b84eeff7d7b7bfb`](https://testnet.arcscan.app/address/0x07fb4b5d6b91f78163649d563b84eeff7d7b7bfb) passes `owner()` = `0x8CF026a2cD40E89f76200168A1b38148B8E0d25C`, broadcaster, recovery, timelock 120 s, all three interfaces, and permissioned registry checks.
- The readback kill is green and links each library plus the account to Arcscan. It reports `PARTIAL G7-copyblox` because no live CopyBlox address is pinned yet.

### Regressions — PASS

- Remote fixture smoke: PASS; `owner()`, broadcaster, recovery, timelock, interfaces, and permissioned registry checks remain green on `1337`.
- Existing U5 live regression: PASS. `u5-mtrduov4.branchzero.eth` minted and resolved on Sepolia to AccountBlox `0x494fDA06F789cCCD53363c512A1d808f7511eBfd`; the resolved address was paid on Remote EVM 1337 in tx `0xf8670a42304e85dea6e6cdcc07f7e97fee778eb2eb9139aac1bdf1632687d363`; duplicate claim was refused `NAME_TAKEN`.
- TypeScript check: PASS for shared, infra, web, and Teller Desk using the installed `tsc` binaries.
- Godot 4.5 `run_checks.gd` was not runnable on this host because no Godot binary is installed. Run `godot --headless --path apps/game -s tests/run_checks.gd` on the documented Godot 4.5.2 host before calling the freeze fully verified.

## Named blocker and fallback

The workspace `.env` has Remote/ENS/Privy values but no `ARC_DEPLOYER_PK`, `ARC_BROADCASTER_PK`, `ARC_MANAGER_PK`, `ARC_OWNER_ADDRESS`, or `ARC_RECOVERY_ADDRESS`. The Arc faucet requires reCAPTCHA, so the agent cannot self-fund or use the ENG-0006 lab throwaway key. The fallback is deliberately fail-closed: keep the ENG fixture as readback evidence, ship the target-specific CopyBlox/provision path, and defer the live product clone/Lane A/B/manager execution kill until an operator supplies separate funded Arc keys.

Operator continuation:

```text
BLOXCHAIN_PROTOCOL_DIR="D:/My Git Projects/ParticleCS/Bloxchain-protocol" node --experimental-strip-types infra/scripts/bootstrap-blox.ts --chain arc
CHAIN_ID=5042002 PORT=8788 node --experimental-strip-types apps/teller-desk/src/server.ts
```

The first command requires a built protocol checkout and `ARC_DEPLOYER_PK`; the second requires the remaining Arc service keys and existing Privy values. After CopyBlox is pinned, run `infra/scripts/arc-kill.ts`, then the product Arc provision + one Lane A kill, and finally the existing U4+ Priority regression against Arc. Do not use Remote keys on Arc.

Next unit is **not advanced**: G7 remains open until the fresh product-controlled Arc clone/manager kill and Godot-host check are completed. See [`docs/HANDOFF-CC.md`](../HANDOFF-CC.md) §5g and [`docs/REFLECTION.md`](../REFLECTION.md) §8.
