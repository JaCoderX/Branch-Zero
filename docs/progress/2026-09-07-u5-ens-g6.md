---
date: 2026-09-07
unit: U5 ENS
gate: G6
result: met — live Sepolia mint/resolve and Remote EVM 1337 pay-by-name; duplicate refused
---

# 2026-09-07 — U5 ENS / G6

## Verdict

G6's product path is live. The kill test used the product ENS module, not MockChain:

| Check | Result | Evidence |
|------|--------|----------|
| Availability | PASS | `u5-mtra3lb6.branchzero.eth` reported available on Sepolia |
| Mint | PASS | `0x96098c6f7d1318d67cdae4259bebea85766b7e5e9ba7266ce984f675a59a023d` (register), `0x48f09e14e2f97bba157a11fdad6c7b21b509f61ae8b80dc6acd8a8d0cd16ef25` (setAddr), `0x35a9a716dc74255c41ce7b8b8dec48371a09276b97789b7eec0b03568f4ff08b` (Silver text) |
| Resolve | PASS | `u5-mtra3lb6.branchzero.eth` → AccountBlox `0x494fDA06F789cCCD53363c512A1d808f7511eBfd` via pinned UR V2 `0x85edf8b6b7d4211e2b07aa687506b746357b92cf` |
| Pay by name | PASS | Remote EVM 1337 Lane A transaction `0x7ea8e3972e33ca8d1e82a8a60b9a39f0689cecf987fef9fc8c87365e68466a58`, destination was the resolved AccountBlox |
| Taken name | PASS | A second mint of the same label returned `NAME_TAKEN` |

The ENS sender was the env-only `ENS_REGISTRAR_PK`; its derived address matched the pinned registrar
`0xc4d7cCabc561c7D9360481404DA8A80886a49277`. The mainnet wallet was not used. The payment transaction remained on
Remote EVM 1337; ENS only supplied the destination address.

## Product changes

- Teller Desk `/ens/available`, `/ens/claim`, `/ens/record`, `/ens/resolve` use a lazy viem Sepolia client and the
  pinned ENSv2 deployment in [`infra/deployments/sepolia.json`](../../infra/deployments/sepolia.json).
- The bridge is `u5.0`: `ensAvailable`, `ensMint`, `ensSetText`, and `resolveName`. Fetch errors retain their desk code;
  ENS writes return focus to the Godot canvas in `finally`.
- Petra, the claim form, the ENS names board, and pay-by-name resolve → address → unchanged Lane A/B are wired through
  `Dialogue → GameState.run_action → Chain.call_async`. MockChain covers greybox only.
- New error codes have bank lines in `apps/game/dialogue/errors.json`; no runtime dependency was added.

## Verification and limitation

- `npm run typecheck` passed for shared, infra, web, and Teller Desk (invoked through the installed Node npm CLI because
  the PowerShell `npm` shim is broken).
- `git diff --check` passed.
- The normal `tsx` U5 command reaches a host `uv_os_get_passwd returned ENOMEM` failure before loading app code. The same
  script passed with Node 22's built-in `--experimental-strip-types` runner after external RPC access was approved:

  `node --env-file=../../.env --experimental-strip-types --no-warnings scripts/kill-tests-u5.ts`

- Godot 4.5 is not installed at the documented host path and is not on PATH, so `tests/run_checks.gd` could not be run
  here. The dialogue JSON and static bridge/route checks were inspected; rerun the headless check on a Godot 4.5 host
  before the next filmed build.

## Follow-ups (not blockers for advancing HANDOFF)

1. On a host with Godot 4.5: run `tests/run_checks.gd` headless (project’s documented runner).
2. If host `tsx` hits `uv_os_get_passwd` / ENOMEM, use:
   `node --env-file=../../.env --experimental-strip-types --no-warnings scripts/kill-tests-u5.ts`

## Handoff

U5 is complete for G6. The next construction unit is U6 Arc + manager role (G7); see
[`docs/KICKOFF-U6.md`](../KICKOFF-U6.md). Do not merge the GameLab ENG tree or redesign the Name Desk.
