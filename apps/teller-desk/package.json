{
  "name": "@branch-zero/teller-desk",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Teller Desk service (Fastify). U0: stub with /healthz against Remote EVM. Signing + broadcasting lanes arrive in U1/U2.",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/server.ts",
    "start": "tsx --env-file-if-exists=../../.env src/server.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@bloxchain/sdk": "1.0.0",
    "@branch-zero/shared": "*",
    "fastify": "^5.1.0",
    "viem": "2.50.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
